// Shared types and wire protocol for the ron-bot audio streaming feature.
// Populated in Phase 4 (companion client <-> bot streaming).

/** Bumped when the client<->bot wire protocol changes in a breaking way. */
export const PROTOCOL_VERSION = 1;
